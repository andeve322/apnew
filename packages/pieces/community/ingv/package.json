{
    "name": "@activepieces/piece-ingv",
    "version": "0.0.1",
    "dependencies": {
        "@activepieces/pieces-framework": "workspace:*",
        "@activepieces/shared": "workspace:*",
        "@activepieces/pieces-common": "workspace:*"
    },
    "scripts": {
        "build": "tsc -p tsconfig.lib.json && cp package.json dist/"
    }
}
